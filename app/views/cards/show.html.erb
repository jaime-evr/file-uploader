<h2><%= @card.name %></h1>

<p><%= @card.note %></p>
<p><%= image_tag @card.image_url.to_s %>
<%= link_to 'back', root_path %>
<%= link_to 'edit', edit_card_path(@card) %>
<%= link_to 'delete', card_path(@card), method: :delete, data: { confirm: 'Are you sure?' } %>

